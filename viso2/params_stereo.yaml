nms_n:
nms_tau:
match_binsize:
match_radius:
match_disp_tolerance:
outlier_disp_tolerance: 
outlier_flow_tolerance:
multi_stage:
half_resolution:  
refinement:
max_features: 
bucket_width:
bucket_height:
max_features:
bucket_width:
bucket_height:

queue_size: 10
approximate_sync: true
